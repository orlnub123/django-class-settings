# TODO: Move to pyproject.toml once native TOML support gets added (#999)
[tox]
envlist = py{35,36,37,38}
isolated_build = true

[testenv]
commands_pre = flock /tmp/django-class-settings.lock poetry install
commands = pytest {posargs}
whitelist_externals = flock
deps = poetry
